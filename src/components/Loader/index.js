import React from 'react';
import './Loader.scss'

const Loader = (props) => {
    return (
        <div class="loader-box">
            <div class="loader"></div>
            Chargement...
        </div>
    );
}

export default Loader;